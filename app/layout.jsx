export const metadata = {
    title: "CanopyLink — Packaging Cost Calculator",
    description: "Find out how much you're overpaying on secondary packaging vs fair market pricing.",
};

export default function RootLayout({ children }) {
    return (
          <html lang="en">
                <body>{children}</body>
          </html>
        );
}
